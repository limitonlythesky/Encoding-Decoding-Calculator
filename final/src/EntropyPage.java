import javax.swing.*;

public class EntropyPage extends JPanel {
    private MainFrame parent;

    public EntropyPage(MainFrame parent){
        this.parent = parent;

        setSize(600, 600);
        setLayout(null);
    }
}
